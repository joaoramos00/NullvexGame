.env
export/
!export/web/
*.tmp
.claude/settings.local.json
.mcp.json
characters/bosses/intro_boss/.pixellab_ids

# >>> OpenCastle managed (do not edit) >>>
CLAUDE.md
.claude/agents/
.claude/skills/
.claude/commands/
!.opencastle/
!.mcp.json
# <<< OpenCastle managed <<<

# StageBot — artefatos de saída (regerados a cada run)
tests/bot/shots/
tests/bot/results/
tests/bot/node_modules/
.worktrees/

# Caches do Godot (regerados na importação)
.godot/

# Tooling local
.superpowers/

# Rascunho/debug na raiz (screenshots, temporários)
/*.png
/*.png.import
/*.gif
/*.gif.import

# Re-ignora .mcp.json (contém Bearer token) — sobrepõe o !.mcp.json do bloco OpenCastle
.mcp.json

# Build web exportado na pasta errada (o correto é export/web/)
exports/

# Python cache
__pycache__/
